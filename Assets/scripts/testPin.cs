using UnityEngine;
using System.Collections;

public class testPin : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Pin pin1 = Instantiate(Pin.pinPrefab) as Pin;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}

using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class LogHighlighted : MonoBehaviour {

	Button buttonscript;
	// Use this for initialization
	void Start () {
		buttonscript = GetComponent<Button> ();
	}
	
	// Update is called once per frame
	void Update () {
//		if (buttonscript.spriteState.highlightedSprite) {
//		}
//		if (buttonscript.) {
//		}
	}
}
